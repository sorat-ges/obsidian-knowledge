{
  "name": "gus-knowledge",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "npm run docs:build && node --test --test-concurrency=1",
    "docs:dev": "astro dev",
    "docs:check": "node tools/validate-docs.mjs",
    "docs:build": "npm run docs:check && astro build",
    "docs:local": "npm run docs:build && astro preview"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.41.4",
    "astro": "^7.1.3",
    "remark-parse": "^11.0.0",
    "unified": "^11.0.5",
    "unist-util-visit": "^5.1.0",
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "parse5": "^8.0.1"
  }
}
